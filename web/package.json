{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm -F @my/grpc build",
    "dev": "vite",
    "prebuild": "pnpm -F @my/grpc build",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@my/grpc": "workspace:*",
    "@protobuf-ts/grpcweb-transport": "^2.10.0",
    "@protobuf-ts/runtime": "^2.10.0",
    "@protobuf-ts/runtime-rpc": "^2.10.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}